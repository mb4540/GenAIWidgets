Rename "AI Gateway Chat" menu item to "Compare AI Models"
Create a new menu item called "AI Chat"
    in this new page allow user to select model provider and model
    must us netlify AI gateway for this
    Add a chat history similar to what we have in Agents Chat page
In the Admin Dashboard add a new tab called "Blob Storage". I want CRUD operations for this.
    Netlify Blobs shows three objects I wand CRUD on each of them:
        ai-chats-jobs
        extracted-chuncks
        user-files
I need a new Agent Tool that is called Web Search
    The user should be able to enter a URL and a search query
    The agent should use the netlify AI gateway to call the web search tool
    The agent should return the results to the user